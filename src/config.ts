import 'dotenv/config';
import { z } from 'zod';
export const config = z.object({ DATABASE_URL:z.string(), REDIS_URL:z.string().default('redis://localhost:6379'), GMI_API_KEY:z.string().default(''), GMI_BASE_URL:z.string().url().default('https://api.gmi-serving.com'), MINIMAX_MODEL:z.string().default('music-3.0'), MEDIA_DIR:z.string().default('./data/media'), PORT:z.coerce.number().default(3000), AUTONOMOUS:z.string().default('false').transform(v=>v==='true'), TARGET_BUFFER_MINUTES:z.coerce.number().default(60), MIN_BUFFER_MINUTES:z.coerce.number().default(30), BACKGROUND_PATH:z.string().default('./assets/background.jpg'), YOUTUBE_RTMPS_URL:z.string().default('rtmps://a.rtmps.youtube.com/live2'), YOUTUBE_STREAM_KEY:z.string().default('') }).parse(process.env);
